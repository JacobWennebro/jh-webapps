{
  "name": "jh-bio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:sass": "sass --watch src/sass:docs/css",
    "build:sass": "sass src/sass:docs/css",
    "watch:eleventy": "env-cmd eleventy --serve",
    "build:eleventy": "env-cmd eleventy",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^1.0.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.51.0",
    "spotify-web-api-node": "^5.0.2"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0"
  }
}
